# 高中地理动画教学

> 面向中国高中地理必修一内容的交互式动画教学应用

## 🚀 项目特点

- 📱 **多端支持**：Web (H5) + 微信小程序
- 🎬 **交互式动画**：帮助学生直观理解地理概念
- 🎨 **教学友好**：柔和配色、清晰对比、适合课堂投屏
- ⚡ **自动部署**：GitHub 推送后自动部署到 Cloudflare Pages

## 📚 内容模块

### 选修一：自然地理基础
- [x] 黄赤交角（Obliquity of the Ecliptic）
- [ ] 四季变化
- [ ] 太阳直射点移动
- [ ] 更多内容开发中...

## 🛠️ 技术栈

- **前端框架**：React 18 + TypeScript
- **UI 组件库**：Material-UI (MUI)
- **构建工具**：Vite
- **小程序框架**：Taro（规划中）
- **部署平台**：Cloudflare Pages

## 📦 项目结构

```
geography-study-react/
├── src/
│   ├── pages/                    # 页面组件
│   │   └── elective1/           # 选修一：自然地理基础
│   │       └── ObliquityOfEclipticDemo.tsx  # 黄赤交角演示
│   ├── shared/                   # 共享资源
│   │   ├── components/          # 通用组件
│   │   ├── hooks/               # 自定义 Hooks
│   │   └── utils/               # 工具函数
│   ├── App.tsx                  # 应用主组件
│   └── main.tsx                 # 入口文件
├── .github/
│   └── workflows/               # GitHub Actions
│       └── deploy.yml           # Cloudflare 部署配置
├── weapp/                        # 微信小程序项目（Taro）
│   └── ...                      # 小程序源码
└── dist/                        # 构建输出目录
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 本地开发

```bash
# Web (H5) 开发
npm run dev

# 微信小程序开发（需要先安装 Taro CLI）
npm run dev:weapp
```

### 构建

```bash
# 构建 Web 版本
npm run build

# 构建微信小程序
npm run build:weapp
```

### 预览

```bash
npm run preview
```

## ☁️ 部署

### Cloudflare Pages 自动部署

项目已配置 GitHub Actions，推送到 `main` 分支后会自动部署到 Cloudflare Pages。

**首次配置步骤**：

1. 在 [Cloudflare Dashboard](https://dash.cloudflare.com/) 创建 Pages 项目
2. 选择 "Connect to Git" 连接 GitHub 仓库
3. 配置构建设置：
   - **Framework preset**: None
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
4. 或者在 GitHub 仓库设置中添加以下 Secrets（使用 GitHub Actions）：
   - `CLOUDFLARE_API_TOKEN`: Cloudflare API Token
   - `CLOUDFLARE_ACCOUNT_ID`: Cloudflare Account ID

### 微信小程序发布

1. 运行 `npm run build:weapp` 构建小程序
2. 使用微信开发者工具打开 `weapp/dist` 目录
3. 上传并提交审核

## 🎯 开发指南

### 添加新的动画组件

1. 在对应的 `src/pages/` 目录下创建组件
2. 在 `App.tsx` 中添加路由
3. 确保组件支持无障碍（aria-label）
4. 添加响应式布局支持

### 代码规范

- 使用 TypeScript 编写所有代码
- 组件使用函数式组件 + Hooks
- 遵循 ESLint 规则

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！
